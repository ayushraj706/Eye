name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Final: Saare dependencies aur naya PostCSS plugin install kar rahe hain
      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps
          npm install -D tailwindcss postcss autoprefixer @tailwindcss/postcss --legacy-peer-deps
          npm install @capacitor-community/keep-awake --legacy-peer-deps

      - name: Build Web (Next.js)
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Build APK (Release)
        working-directory: ./android
        run: ./gradlew assembleRelease

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: android/app/build/outputs/apk/release/
